// Resident Routes placeholder
const express = require('express');
const router = express.Router();
const residentController = require('../controllers/residentController');

// Define resident routes here

module.exports = router;
