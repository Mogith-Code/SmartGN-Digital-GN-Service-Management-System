// Allowance Routes placeholder
const express = require('express');
const router = express.Router();
const allowanceController = require('../controllers/allowanceController');

// Define allowance routes here

module.exports = router;
